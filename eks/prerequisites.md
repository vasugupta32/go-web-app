Prerequisites

Before proceeding, make sure the following tools are installed and properly configured:

kubectl
Command-line tool for managing Kubernetes clusters and deploying applications.

eksctl
Utility for creating and managing Amazon EKS clusters with minimal manual setup.

AWS CLI
Interface for interacting with AWS services, including Amazon EKS.
After installation, run aws configure to set up your credentials and default region.